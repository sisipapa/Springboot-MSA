# contents-service bootstrap.yml
server:
  port: 8081
spring:
  config:
    import: optional:configserver:http://localhost:9000
#spring:
#  profiles:
#    active: local
  application:
    name: resource2
management:
  endpoints:
    web:
      exposure:
        include: info, refresh