server:
  port: 8080
spring:
  application:
    name: resource
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: config
        enabled: true
    # uri: http://localhost:9000
management:
  endpoints:
    web:
      exposure:
        include: info, refresh
logging:
  file:
    path: C://logs/resource
  level: debug


#  cloud:
#    config:
#      uri: http://localhost:9000
#      fail-fast: true
#      discovery:
#        service-id: config-service
#        enabled: true